{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "installCommand": "apt-get update && apt-get install -y cmake && pip install -r requirements.txt"
}
